#include <iostream>
#include <vector>
#include <fstream>
#include <ctime>
using namespace std;

int iterativeSearch(vector<int>v, int elem){
    int i;
    for(i = 0; i = SIZE; i++){
        if(v[i] == elem){
            return i;
        }
    }
    return -1;
}

int binarySearch(vector<int> & v, int start, int end, int elem){
    int mid;
    if(elem = mid){
        return -1;
    }else{
        
    }
}